# Security

Do not commit real credentials. Use `.dev.vars.example` for variable names and set real values in Cloudflare or local `.dev.vars`.

CI runs typecheck, tests, and production dependency audit on every push and pull request.

Report security issues privately to the repository owner. Rotate any exposed token before opening a public issue or pull request.
